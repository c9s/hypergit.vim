# vim:fdm=expr:fdl=1:foldexpr=getline(v\:lnum)=~'^=='?'>1'\:getline(v\:lnum)[0]=~'[*x(]'?'>2'\:getline(v\:lnum)=~'^--'?'<2'\:'='

* libnotify integration

* zenity integration

* sm (simple message) integration

* auto checkout 
    show a branch list to select.

* karam format:
    [+++++         ] K40.00
* Gpush , Gpull command completion
* support wildchar when adding files.
* git action should respect to the current editing buffer.
* improve git rebase --interactive ui
